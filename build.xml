<!-- ====================================================================== 
	 Tools build system.
	  
	 $Id: build.xml,v 1.6 2005/06/27 11:28:41 dron Exp $
     ====================================================================== -->
<project name="tplParser" default="all">
  
  <!-- Cleaning up output location. -->
  <target name="clean">
    <delete dir="build"/>
  </target>
    
  <!-- Prepare for build: create output folders -->
  <target name="pre-build-make-folder">
    <mkdir dir="build"/>
  </target>
    
  <!-- Build all java sources. -->
  <target name="tplParser-build" depends="pre-build-make-folder">
    <javac srcdir="org" destdir="build"/>
  </target>
  
  <!-- Generate release JAR package. -->
  <target name="tplParser-jar" depends="tplParser-build">
    <jar basedir="build" destfile="tplParser.jar">
      <manifest>
        <attribute name="Built-By" value="${user.name}"/>
        <attribute name="Main-Class" value="org.valabs.tplParser.TplParser"/>
      </manifest>
    </jar>
  </target>
    
  <!-- Default target: build jar file. -->
  <target name="all" depends="tplParser-jar"/>
</project>